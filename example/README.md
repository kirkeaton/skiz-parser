# skiz-parser-example

Basic working example of skiz-parser

## Requirements

- `pnpm`
- Node.js >= 18

## Prerequisites

- Install dependencies by running `pnpm install`

## Running

```bash
$ pnpm start
```
